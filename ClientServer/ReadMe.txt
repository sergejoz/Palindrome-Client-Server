 лиент-сервер приложение дл€ определени€ палиндромов. 

 лиент
-  лиент и сервер взаимодействуют по сети
-  лиент при запуске принимает путь до директории с входными данными
- ¬ходные данные имеют вид текстовых файлов произвольной длины
- ‘айлов может быть от 0 до 127
- «адача клиента отправить текст каждого файла на сервер дл€ проверки и получить ответ в формате True/False
- ѕолученные ответы  лиент может предоставить в любом человекочитаемом  формате, например: вывести в консоль (или GUI) в ходе работы приложени€, сохранить в файл по итогам работы приложени€ (файл сохран€етс€ в \ClientServer\ClientServer\bin\Debug\log.txt).
-  лиент успешно решил поставленную перед ним задачу, если в выводе есть корректные ответы от —ервера по каждому файлу.
-  лиент знает о том, что —ервер может обрабатывать несколько запросов за раз, но не знает сколько конкретно. » стремитс€ использовать возможности сервера максимально.

—ервер
- —ервер и клиент взаимодействуют по сети
- —ервер принимает вход€щие запросы от  лиента и провер€ет €вл€етс€ ли присланный текст палиндромом. ≈сли текст €вл€етс€ палиндромом, то —ервер возвращает ответ True, иначе False
- “ак как вычисление палиндрома задача дл€ —ервера творческа€, то он никак не может затратить на нее меньше 1 секунды (допустимо использование методов Thread.Sleep или Task.Delay)
- —ервер может обрабатывать за раз только N запросов, и в случае если свободных потоков дл€ обработки нет, вернуть клиенту ошибку. N - €вл€етс€ целым числом от 1 до 10 и задаетс€ на форме сервера

